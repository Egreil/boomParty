{% block body %}
    <h2>Modification de vos informations</h2>
    <div>
        <div>
{#            <img src="{{ image }}" alt="image de profil de {{ participant.pseudo }}">#}
            <p>Nom:</p>
            {{ participant.nom }}
            <p>Prenom:</p>
            {{ participant.prenom }}
            <p>Email:</p>
            {{ participant.email }}
            <p>Téléphone:</p>
            {{ participant.telephone }}
            <p>Campus:</p>
            {{ participant.campus.nom }}
            <p>Pseudo:</p>
            {{ form_row(form.pseudo) }}
            {{ form_row(form.image) }}
            <form>
                <div>
                {% if app.user %}
                    <div>
                        <label for="ancienMDP">Ancien mot de passe :</label>
                        <input id="ancienMDP" type="password">
                    </div>
                {% endif %}
                    <div>
                        <label>Nouveau mot de passe</label>
                        <input id="ancienMDP" type="password">
                    </div>
                    <div>
                        <label>Confirmation du mot de passe</label>
                        <input id="ancienMDP" type="password">
                    </div>


            </form>
        </div>
    </div>
{% endblock %}